services:
  mosajjil:
    build: .
    container_name: mosajjil
    ports:
      - "8080:3000"
    restart: unless-stopped
    volumes:
      - downloads:/app/downloads
      - ./cookies.txt:/app/cookies.txt
    environment:
      - NODE_ENV=production
      - PORT=3000

volumes:
  downloads:
